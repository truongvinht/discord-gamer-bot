// reactionHandler.js
// handle reactions on message
// ==================

const reaction = (reaction, user) => {
    console.log('Test Reaction');
    // reaction for figure draft
};

// export
module.exports = {
    messageReaction: reaction
};
